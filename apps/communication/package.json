{
  "name": "communication",
  "version": "0.1.0",
  "scripts": {
    "dev": "tsc -b && node ./dist/index.js",
    "build": "tsc -b",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "@repo/redisdb": "*",
    "@repo/constructors": "*",
    "@repo/types": "*",
    "typescript": "^5.7.2",
    "ws": "^8.18.0"
  }
}
